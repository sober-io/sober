//! `soberctl` — runtime CLI for agent inspection, task queue management,
//! scheduler control, and live health checks via Unix admin sockets.

fn main() {
    println!("soberctl: not yet implemented");
}
